<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    {{!-- <link rel="stylesheet" type="text/css" href="/css/homepage.css"/> --}}
    <link rel="stylesheet" type="text/css" href="/css/dashboard.css">
</head>
{{>primary-header}}
{{>modals}}

<main>

<header id="dashboard-btn">
    <button id="make-post" type="button" class="btn btn-primary btn-lg">New Post</button>
</header>
{{>editPost}}

<div id="my-post-card" class="card ">
  <div class="card-header">
    My Posts
  </div>
    {{#each posts }}
  <div class="card-body my-post-card-body">
    <div id="{{id}}" class="card blog-post-dashboard">
        <h5 id="title-{{id}}" class="card-header post-card-header">post: {{post_title}}</h5>
        <h5 id="post-data-{{id}}" class="card-header post-card-header post-card-data">{{set_date post_date}}</h5>
    </div>
</div>
    {{/each}}
  </div>

</div>

{{!-- <div id="my-comment-card" class="card">
  <div class="card-header">
    My Comments
  </div>

    {{#each comments as |aComment|}}
  <div id="my-comment-card-body" class="card-body">

    <div id="comment-{{aComment.id}}" class="card post-comment-dashboard">
        <h5 id="comment-header-{{aComment.id}}" class="card-header">Featured</h5>
        <button id="comment-edit-{{aComment.id}}" class="btn btn-primary edit-comment" style="margin: 10px;">Edit</button>
        <button id="comment-delete-{{aComment.id}}" class="btn btn-primary delete-comment" style="margin: 10px;">Delete</button>
</div>
    
    {{/each}}

  </div>
</div> --}}


</main>




<script src="/js/blogContent.js"></script>
<script src="/js/comments.js"></script>
<script src="/js/comments_db.js"></script>
<script src="/js/dashboard.js"></script>
<script src="/js/editPost.js"></script>
