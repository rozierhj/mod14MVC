<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/homepage.css"/>
</head>
{{> primary-header}}

<main>
{{> createPost}}
{{> comments}}
<header>
    <button id="make-post" type="button" class="btn btn-primary btn-lg">New Post</button>
</header>
<div>
    {{#if posts}}
    {{#each posts }}
       
    <div id="{{id}}" class="card blog-post">
        <h5 id="header-{{id}}" class="card-header post-header">post by {{user_name}} on {{set_date post_date}}</h5>
        <div class="card-body">
        <h5 id="post-title-{{id}}" class="card-title">{{post_title}}</h5>
        <p id="post-text-{{id}}" class="card-text">{{blog_post}}</p>
        <div></div>
        <button id="delete-{{id}}" class="btn btn-primary delete-post">Delete</button>
        <button id="edit-{{id}}" class="btn btn-primary edit-post">Edit</button>
        <button id="edit-{{id}}" class="btn btn-primary comment-button">Comments</button>
    </div>
</div>
    {{/each}}
    {{/if}}
</div>

</main>


{{!-- <script src="/js/homepage.js"></script>
<script src="/js/comments.js"></script>
<script src="/js/blogContent.js"></script> --}}